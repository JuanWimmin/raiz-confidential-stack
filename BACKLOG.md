# BACKLOG — ideas fuera del sprint

> Regla de oro: toda idea nueva viene AQUÍ, nunca al sprint. Se revisa DESPUÉS
> de la submission (6-ago). Este archivo alimenta el SCF de octubre.

| Fecha | Idea | Origen | Notas |
|---|---|---|---|
| 2026-08-03 | raiz-memory: backfill opcional por contrato (`CONTRACT_START_LEDGERS`) — hoy el primer arranque empieza en el ledger actual (ingest.rs:33-38) y los eventos previos del contrato, aunque sigan vivos en la ventana RPC, no se archivan | Sesión 4 (la DB dedicada del test no vio los eventos del run 1) | Mitigación actual: arrancar la instancia de producción apenas se despliega un contrato |
| 2026-08-03 | goal_meta: añadir `set_view_key` y `archive_goal` (hoy no hay setter — el goal 0 placeholder quedó muerto y hubo que crear el goal 1) | Sesión 7 | Candidato a Sesión 8 si sobra tiempo; con tests de auth |
| 2026-08-03 | receipt: pasar Raiz Memory como `indexer` param del verificador de disclosure (hoy relee eventos del RPC directo) | Sesión 7 | 1 línea de config; refuerza la sinergia |
| 2026-08-04 | Meta: `Actualizar` refresca el timeline pero deja el total del hero clavado en la verificación anterior (`MetaViewModel.refresh()` usa `autoVerify=false` por defecto, `SobreApp.onActualizar` no pasa nada) — tras un aporte quedan en pantalla "4 aportes" y "Verificado … ledger <viejo>" a la vez, y hace falta un segundo tap en `Verificar de nuevo` | Ensayo del demo (Sesión 6) | El video necesita dos taps donde uno bastaría. Arreglo probable: `refresh(autoVerify = true)` desde el icono del header, o marcar el total como "desactualizado" cuando cambia el timeline |
| 2026-08-04 | Mi sobre: la tarjeta de resultado con el hash de la tx se dibuja DEBAJO de los botones y queda fuera de pantalla en 720×1600 justo al terminar la operación — hay que hacer scroll para ver el hash que se acaba de generar (el `ProofProgress` sí está above the fold, su sucesor no) | Ensayo del demo (Sesión 6) | Simétrico al fix ya hecho para el progreso: mover `op-result` arriba mientras `op is Ok` y sea reciente |
| 2026-08-04 | Mi sobre: el teclado numérico tapa `Sellar`/`Aportar` en 720×1600; hay que cerrarlo con BACK antes de poder tocar el botón | Ensayo del demo (Sesión 6) | `imeAction=Done` + `Modifier.imePadding()` o scroll automático al foco |
| 2026-08-04 | Meta: el hero imprime "N eventos de **events-url**" — string de máquina del shim filtrándose a copy en español; debería decir "Raiz Memory" o "RPC (simulado)" | Ensayo del demo (Sesión 6) | Cosmético pero sale en primer plano en el beat 2 del video |
| 2026-08-04 | raiz-memory: `RETENTION_SIMULATION_LEDGERS=2000` (~2 h 47 min) hace que el aporte hecho EN VIVO durante el demo sobreviva a la purga simulada (5 aportes → 2, no → 0) | Ensayo del demo (Sesión 6) | Se decide dejarlo en 2000: la historia real ("el RPC recuerda esta mañana, no recuerda la creación de la meta") es más honesta que un timeline vacío. Anotado por si se quiere una ventana más agresiva para el video |
| 2026-08-04 | Herramienta externa (no CT/SPP): en este Vivo Y21 con Android 13 `adb logcat` solo entrega líneas de nivel E al host — el `Log.i(TAG, "$method SUCCESS tx …")` de `CtWallet` nunca llega. Cualquier medición o captura automatizada en este teléfono tiene que ir por `uiautomator dump` (~1.3 s por muestra) | Ensayo del demo (Sesión 6) | No es bug nuestro; queda documentado en docs/demo-run.md para no volver a perder tiempo |
